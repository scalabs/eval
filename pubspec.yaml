name: eval
description: Pure Dart LLM evaluation helpers for tests, including judge-based matchers, RAG scoring, and statistics.
version: 0.0.4

repository: https://github.com/scalabs/eval
homepage: https://biralo.studio
issue_tracker: https://github.com/scalabs/eval/issues

topics:
  - llm
  - evaluation
  - testing
  - ai
  - rag

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.6.0
  matcher: ^0.12.0
  test: ^1.28.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
